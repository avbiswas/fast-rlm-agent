# Synthesize several files

This case exercises structured file extraction. Paste the contents of
`PROMPT.txt` directly into the harness. The referenced files should arrive in
the model context as structured `{path, content}` entries.
