# Research several URLs

This case exercises structured URL extraction and the harness's fetch tool.
Paste the contents of `PROMPT.txt` directly into the harness. The agent should
fetch each source and create `rust-memory-guide.md`.
